% FORMALISATION RATISS-FOCAL — Focalisation informationnelle (CANONIQUE v3)
% Règle : chaque équation porte son test en commentaire. Pas d'équation sans test.
% Posé par Jonathan Evina, nuit du 21 sept. 2026. Compiler : pdflatex (ou Overleaf).
\documentclass[11pt,a4paper]{article}
\usepackage[french]{babel}
\usepackage{amsmath,amssymb}
\usepackage[margin=2.4cm]{geometry}
\title{RATISS-FOCAL : formalisation de la focalisation informationnelle}
\author{RATISS Labs --- Jonathan Evina}
\date{21 septembre 2026 (v3 : théorie + mesures + durcissement)}
\begin{document}
\maketitle

\section{Question et lois accompagnatrices}
La cohérence émerge-t-elle de l'information ? Deux lois accompagnatrices
fournissent la mesure, sans machinerie neuronale.
\textbf{LCT} : % TEST-01/02
\begin{equation}
R = P_{\mathrm{sig}}, \qquad \Delta W = \eta\,\varphi\,P_{\mathrm{sig}}\,C.
\end{equation}
On certifie la forme, pas le courant.
\textbf{Stine-24} : soit $s_i(t)$ la part de persistance de la couche $i$,
$f_i(t) = 1 - |s_i(t) - s_i(0)|$ la fidélité de couche, alors % TEST-08
\begin{equation}
\Psi_{\mathrm{sync}}(t) = \left(f_1 f_2 f_3\right)^{1/3} \to 1 = \text{accordées}.
\end{equation}
Mesuré : $\Psi = 0.874$ (alignées).

\section{Échelles, couplage, intrication}
Trois couches (macro/micro/info), facteurs internes par couche plus facteur
externe $X$ : % TEST-09
\begin{equation}
X = \tfrac{1}{3}\sum_{i<j} |\mathrm{corr}(P_i(t), P_j(t))|.
\end{equation}
Mesuré : $X = 0.147$ (couplées, surtout macro-micro : $0.33$).
Non-séparabilité : % TEST-10
\begin{equation}
U = P_{\mathrm{joint}} - \sum_i P_i, \qquad |\Psi\rangle \neq |\mathrm{macro}\rangle \otimes |\mathrm{micro}\rangle \otimes |\mathrm{info}\rangle.
\end{equation}
Mesuré : $U_{\mathrm{fond}} = 0$ (contrôle), $U_{\mathrm{chargé}} = -1.36$ :
l'information lie les couches.
Tryperposition : collapse de 30\% d'une couche, sync préservée (ratio $> 0.7$,
TEST-11 ; stable sur 1000 pas, TEST-25).

\section{Influence et séparation}
Taux d'influence (terme d'interaction) : % TEST-12
\begin{equation}
I(X \to Y) = \frac{P_{Y+\mathrm{pont}} - P_Y - P_{\mathrm{pont}}}{A^X}.
\end{equation}
Mesuré : $I_{\mathrm{loin}} \simeq 0$, $I_{\mathrm{pont}} = +0.0025$ :
l'influence exige un contact. Borne $I_{\min} > 0$ : corrélation résiduelle
$0.26$ (bruits indépendants) contre $0.99$ (bruit commun).
Séparateur global/individuel : % TEST-13
\begin{equation}
K(L) = 1 - \frac{P_L}{P_{\mathrm{joint}}}.
\end{equation}
Mesuré : $K = \{0.33, 0.80, 0.67\}$.

\section{Secteurs et constantes}
Une seule loi ne suffit pas : résidus par secteur $/$ résidu global $= 0.001$
(secteurs $S_i$ requis, TEST-14).
Nombres sans dimension du labo : % TEST-15
\begin{equation}
\Pi_c = \frac{\Phi_c}{N\,\tau} = 0.0814, \quad
\Pi_P = \frac{P_{\mathrm{ref}}}{N_{\mathrm{pts}}} = 0.0230, \quad
\Pi_H = H = 0.997,
\end{equation}
stables entre tirages ($< 25\%$). Universalité : question ouverte.

\section{Concentration, porteurs, seuils}
\begin{equation} % TEST-03
\Phi(X) = \frac{N_{\mathrm{int}} \times \tau}{V_{\mathrm{info}}} \geq \Phi_c(X)
\;\Rightarrow\; \text{focalisation}.
\end{equation}
Porteurs : tranches disjointes $m_i \cap m_j = \varnothing$ vérifiées ;
franchissement $\Phi_c^{\mathrm{lab}} = 500$ aux étapes I$=3$, S$=4$.
Seuils par structure : $\sigma_c(\text{boucle}) = 0.02 \neq
\sigma_c(\text{diffus}) = 0.20$ (TEST-16).

\section{Maintien, H2', correspondance}
F0 (focalisation dynamique) : entretenu $0.79$ contre abandonné $0.62$ sous
fort bruit, coût $1920$ bits (TEST-17).
H2' : $\kappa = f(D, I)$, bascule formelle $|R-1| > 0.03$ avec $1$ hors
$[R-2\sigma, R+2\sigma]$ : \textbf{détectée} en 9 cellules (champ proche
$D \leq 2.2$ + forte injection, $R$ jusqu'à $1.11$) --- TEST-24.
Correspondance : injection $\to 0$ = retour au témoin $P = 1.0$, monotone (TEST-19).

\section{Verdicts et durcissement V3}
Anti-triche V3 (diagrammes complets, 3 graines) : % TEST-23
\begin{equation}
\rho = \frac{|\mathrm{zlib}(\mathrm{diagrammes})|}{|\mathrm{zlib}(\mathrm{règles})|}
= 26.29 \pm 1.33 > 2.5.
\end{equation}
Lissage restreint (masque $m_i = \mathbb{1}_{i \geq N_{\mathrm{fond}}}$) :
érosion du fond $= 0.000 < 0.02$ (TEST-21).
Kuramoto $K = 3.0$, $T = 24$ : bascule sync à l'étape 9, finale $0.51$ (TEST-22).
PLV 1000 pas : $0.866$ (var $0.0013$), post-collapse $0.886$ (TEST-25).
Verdict : \textbf{A+B sœurs}. V3 : \textbf{5/5}, base blindée.

\section{Loi de Proximité-Condensation (V4)}
Soit $\rho(X)$ la densité informationnelle locale (noyau gaussien sur le
champ d'info). Le couplage par nœud vaut : % TEST-27
\begin{equation}
K_{\mathrm{eff},i} = K \cdot \frac{\rho_i}{\rho_{\max}} \quad \text{(mesure)},
\qquad K_{\mathrm{eff},i} = K \cdot \mathbb{1}_{\rho_i \geq \rho_c}
\quad \text{(interrupteur, bilatéral : $\min$)},
\end{equation}
avec $\rho_c = 8.01$ EXTRAIT de la mesure (interpolation sync $= 0.3$),
jamais supposé. Vérifié : à $D = 5.0$, nœuds actifs sync $= 0.98$ contre
inactifs $= 0.11$ ; corrélation fraction-sync $= 0.94$. Sans proximité,
même $K = 6.0$ reste quasi-nul (contrôle gaté $0.26$ contre pur $0.72$).

\section{Principe de Résilience Post-Collapse (V4)}
Couple $(C_{\mathrm{pre}}, C_{\mathrm{post}})$, indice : % TEST-28
\begin{equation}
R = \frac{C_{\mathrm{post}}}{C_{\mathrm{pre}}}.
\end{equation}
Mesuré au niveau $P$ : sélection $>$ neutre à fort bruit ($+3$-$4$ pts),
mais $R < 1$ partout (pas de zone $R > 1$) : purification RELATIVE.
Au niveau sync (TEST-25) : $R = 0.886/0.866 = 1.023 > 1$ : purification
ABSOLUE. Domaine de la loi : les liens (sync), pas le niveau.
Verdict V4 : \textbf{2/2}, deux lois codifiées.

\section{Tests d'unification V5}
Subsistance mutuelle des secteurs G (contraction) et Q (Kuramoto) : % TEST-30
\begin{equation}
C(X,Y) = \langle P^{\mathrm{couplé}} - P^{\mathrm{isolé}} \rangle : \quad
C_G = +0.096,\ C_Q = +0.040\ (6/6\ \mathrm{positifs}).
\end{equation}
Non-réduction : $R^2_{\mathrm{RED}} \simeq 0.00$ dans les deux sens, modèle
joint strictement meilleur (ratios $0.84$, $0.45$) --- TEST-31.
Secteurs : S-grav = décroissance linéaire ($R^2 = 0.989$) + noyau résiduel
($0.305$, pente $0.0098$) ; S-quant : forme double-exponentielle % TEST-36
\begin{equation}
r(t) = 1 - a\,e^{-k_1 \tau} - (C_0-a)\,e^{-k_2 \tau}, \quad R^2 = 0.968,
\end{equation}
($a = 0.41$, $k_1 = 0.098$ rapide, $k_2 = 0.001$ lent : deux temps physiques).
Volatilité Q STRUCTURELLE : pente spectrale $-0.72$, autocorr $0.63$
(mémoire, pas bruit blanc) --- TEST-37.
Ligne invisible : seuil $\Phi_c = 100 + 1900\,u(\mathrm{message})$,
coïncidences $30/30$ partagé contre $0.33$ indépendant, robuste sous bruit
Q natif ($28/30$ vs $9/30$, séparation $19$) --- TEST-33/38 : transmission
informationnelle non-géométrique validée.
Marqueurs : causalité $0.67$, référence partagée ; douceur partielle
(volatilité Q = signal, F8) --- TEST-34.
Verdict V5 : \textbf{3/5, partielle}, résidu Q levé en V6 (\textbf{3/3}) :
S-grav = décroissance linéaire + noyau résiduel, variable thermodynamique
dynamique (ni constante, ni propriété topologique irréductible : érodée
par contraction et collapse, cf. §10.ter) ;
S-quant = double-exp($k_1, k_2$) + bruit coloré à mémoire.

\section{Noyau Résiduel Gravitationnel (§10.ter, V7)}
Le plancher de contraction suit une loi exponentielle : % TEST-40
\begin{equation}
G_{\min}(\alpha, \sigma, N) = \exp(-0.29 - 7.16\,\alpha - 6.59\,\sigma
+ 0.0012\,N), \quad R^2 = 0.920.
\end{equation}
Chaque $+0.1$ de contraction DIVISE le noyau par deux ; le bruit l'érode
doucement ; la taille le renforce. Collapse : noyau COUPLÉ à l'intensité
(corr $-0.87$ ; $\Delta G \approx -0.10$ à 10\% $\to -0.21$ à 50\%) :
non invariant --- TEST-41. Choc extrême : réponse PLASTIQUE
($0.345 \to 0.273$ stable, pas d'oscillation ; sursaut d'artefacts pendant
le choc) --- TEST-42.
Verdict V7 : \textbf{3/3} — le noyau suit une loi, s'érode au collapse,
se déforme sans casser au choc.

\section{Mémoire Plastique (§10.quater, V8)}
Hystérésis informationnelle : le choc n'est pas filtré, il BLANCHIT Q
(centroïde spectral $\times 2.4$, autocorr $\div 1.8$, niveau $-2$\%) :
la mémoire lente est détruite, pas sélectionnée --- TEST-44 (hypothèse
passe-bas réfutée). La ligne invisible est un SANCTUAIRE : $28/30$ vs
$9/30$ INCHANGÉS sous choc --- TEST-45 : l'intrication U ne dépend pas
du noyau G. Reconstruction G post-choc (1000 pas) : % TEST-46
\begin{equation}
G(\tau) = 0.095 + 0.34\,e^{-\tau/7.2} + 0.05\,e^{-\tau/400},
\quad R^2 = 0.902,
\end{equation}
vrai plancher $0.095$ (le $0.273$ à 10 pas était un artefact de fenêtre),
temps de cicatrisation $\tau \approx 400$ pas, oscillations amorties
(pic spectral $\times 77$). Q récupère son niveau ($0.789 \to 0.796$) ;
G ne récupère rien ($0.345 \to 0.095$, $-72$\%).
Verdict V8 : \textbf{2/3} — une réfutation franche, un sanctuaire, une
blessure qui saigne 40 fois plus longtemps que le choc.

\section{Antagonisme Q/G (§10.quinquies, V9)}
Q est élastique et blanchissant (homéostatique : revient à son niveau
sans mémoire du choc) ; G est plastique et érosif (garde la blessure).
Sous chocs cumulés, Q_final bascule entre deux attracteurs ($\sim 0.78$
et $\sim 0.87$, dérive $13$\% : FATIGUE au sens classée, mais par
bistabilité, pas par déclin) --- TEST-50. G suit une loi saturante : % TEST-48
\begin{equation}
G_{\min}(N) = 0.083 + 0.26\,e^{-3.0\,N}, \quad R^2 = 0.978,
\end{equation}
noyau absolu $0.083$ : le premier choc effondre ($0.345 \to 0.097$),
les suivants brassent dans une bande stochastique $[0.06, 0.11]$
(non monotone). Il existe un plancher indestructible par accumulation.

\section{Sanctuaire U (§10.sexies, V9)}
L'intrication universelle croisée est indépendante du noyau G :
partage $28/30$ IDENTIQUE sur 7 niveaux de chocs cumulés ($k = 0..6$,
$\Delta S_U = 0$, $N_c$ = néant) --- TEST-49 : SANCTUAIRE ABSOLU.
U n'est pas émergent de la focalisation $\Phi$ ; c'est une propriété
primitive de l'information, antérieure à la gravité informationnelle.
Sanctuaire confirmé absolu sur 7 chocs cumulés --- TEST-49 : robuste
aux chocs discrets, vulnérable au bruit ambiant continu, seuil
quantifié --- TEST-58 :
\begin{equation}
S_U(\sigma) = \mathrm{sigmoïde},\quad \sigma_c = 0.06,\quad R^2 = 0.964,
\end{equation}
plateau $29/30$ ($\sigma \le 0.03$) $\to$ $21/30$ ($\sigma = 0.10$).
Seuil de Bruit Ambiant $\sigma_c = 0.06$. Sanctuaire U conditionnel :
robuste aux chocs discrets, vulnérable au bruit chronique $> \sigma_c$
--- TEST-61/62 (pas de mort à $0.30$, plancher $\sim 30$\%,
irréversible ; couplage à la texture : $+2$/$+4$).
Verdict V9 : \textbf{3/3} — G sature sur un noyau absolu, Q saute
entre attracteurs, U ne cille pas.

\section{Oscillateur Anti-Persistant Symétrique (§10.septies, V10-V12)}
Q n'est pas un stockage discret ; c'est une variable analogique
fluctuante. La « bistabilité » V9 ($n = 6$) ne survit pas à la réplique
($n = 40$) : Q-final post-choc forme un CONTINUUM $[0.76, 0.91]$ sans
trou ($> 0.018$) --- TEST-52 : MONOSTABLE (ni seuil $\phi_c$, ni $R_c$).
Tout choc, même à $0.25\times$ l'intensité standard, fait basculer
HIGH $\to$ LOW ($44/44$) --- TEST-53 : bassin FRAGILE, sans profondeur
mesurable. U faiblement couplé au niveau de volatilité ($\Delta = 3$)
--- TEST-54.
Verdict V10 : \textbf{1/3} — pas d'attracteurs, fragilité totale,
couplage faible Q/U.
Le flip est CONTRARIANT et symétrique : départ HIGH $\to$ médiane
$0.77$, départ LOW $\to$ médiane $0.84$ --- TEST-56 (SYMÉTRIQUE, M25) :
pas de dérive basse, pas de point d'équilibre $\bar{Q}$. Distribution
du re-tirage ($n = 200$) : moyenne $0.816$, écart-type $0.045$, skew
$+0.69$ ; ni gaussienne ni bêta ($R^2 = 0.57$) --- TEST-57.
Verdict V11 : \textbf{2/3} — flip symétrique, distribution rebelle,
seuil U $\sigma_c = 0.06$ (cf. §10.sexies).
Le flip est PILOTABLE par impulsion de phase post-choc (resync forcée)
--- TEST-60 (CONTRÔLABLE, M26) : % TEST-60
\begin{equation}
\phi_{\mathrm{inj}} \in [0, \pi] \Rightarrow \mathrm{LOW}\ (P = 1.0),
\quad \phi_{\mathrm{inj}} \in \{5\pi/4, 3\pi/2\} \Rightarrow \mathrm{HIGH}\
(P = 1.0),
\end{equation}
indépendamment de l'état pré-choc (14/16 cellules ; $7\pi/4$ = zone de
transition, 50\% ambiguë). U ne meurt pas jusqu'à $\sigma = 0.30$
(plancher partiel $\sim 30$\% contre $\sim 16$\% contrôle ; érosion
IRRÉVERSIBLE : 20 pas de calme ne sauvent pas un essai) --- TEST-61
(MIXTE assumé, barrière globale). À $\sigma$ identique, la structure
des fluctuations HIGH porte U à $+2$/$+4$ au-dessus des LOW (2 blocs)
--- TEST-62 : COUPLÉ STRUCTUREL.
Verdict V12 : \textbf{1/3} — flip pilotable, plancher U irréversible,
U lit la texture du bruit.
\end{document}
